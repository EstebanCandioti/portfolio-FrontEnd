import { SortPortOrdenPipe } from './sort-port-orden.pipe';

describe('SortPortOrdenPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPortOrdenPipe();
    expect(pipe).toBeTruthy();
  });
});
